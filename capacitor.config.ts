import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'net.dantemc.jelly',
  appName: 'Jellyfin: Civitas Edition',
  webDir: 'dist'
};

export default config;
